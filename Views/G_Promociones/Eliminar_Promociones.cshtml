@model PromocionesModel
@{
    ViewData["Title"] = "Eliminar_Promociones";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<div class="card">
    <div class="card-header">
        Eliminar Producto
    </div>
    <div class="card-body">


        <form asp-action="Eliminar_Promociones" asp-controller="G_Promociones" method="post">

            <input asp-for="PromocionesId" type="hidden" class="form-control">

            <div class="alert alert-danger" role="alert">
                ¿Desea eliminar el producto : @Html.DisplayTextFor(m => m.NProducto) ?
            </div>

            <button type="submit" class="btn btn-danger">Elimnar</button>
            <a asp-action="Listar_GPromociones" asp-controller="G_Promociones" class="btn btn-warning">Volver a la lista</a>
        </form>


    </div>
</div>

