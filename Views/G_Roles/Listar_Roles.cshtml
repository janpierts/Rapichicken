@model List<InventarioModel>
@{
    ViewData["Title"] = "Listar_Roles";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="card">
  <div class="card-header">
    Gestionar Roles
  </div>
  <div class="card-body">
    <a asp-action="FGuardar_Inventario" asp-controller="G_Roles" class="btn btn-success"><i class="bi bi-plus-circle"></i> Crear Nuevo</a>
    <hr />
    <table class="table table-dark table-striped">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre Rol</th>
                <th>Descripcion</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.InventarioId</td>
                        <td>@item.NProducto</td>
                        <td>@item.Descripcion</td>
                        <td>
                            <a asp-action="FEditar_Inventario" asp-controller="G_Roles" class="btn btn-primary btn-sm" asp-route-I_ID="@item.InventarioId">Editar</a>
                            <a asp-action="Eliminar_PInventario" asp-controller="G_Roles" class="btn btn-danger btn-sm" asp-route-I_ID="@item.InventarioId">Eliminar</a>
                        </td>
                    </tr>

                }
        </tbody>
    </table>
        

  </div>
</div>

